{
  "private": true,
  "name": "rjs-dev-fixture",
  "dependencies": {
    "requirejs": "^2.3.2",
    "rjs-dev-fixture": "file:"
  },
  "scripts": {
    "build": "rm -f output.js && r.js -o config.js"
  }
}
